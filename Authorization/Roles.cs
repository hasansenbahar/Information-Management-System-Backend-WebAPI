namespace WebService.API.Authorization
{
    public enum Roles
    {
        SuperAdmin,
        Admin,
        Basic
    }

    public enum NormalizedRoles
    {
        SUPERADMIN,
        ADMIN,
        BASIC
    }
}